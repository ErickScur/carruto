<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{title}} - QuizMaster</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body { font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f5f5; } .navbar { background-color: #3498db; } .card
      { border-radius: 10px; box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px; } .btn-primary { background-color: #3498db;
      border-color: #3498db; } .btn-primary:hover { background-color: #2980b9;
      border-color: #2980b9; } .container { padding-top: 30px; padding-bottom:
      30px; } .choice-btn { width: 100%; margin-bottom: 10px; text-align: left;
      padding: 15px; font-size: 1.1em; transition: all 0.2s; } .choice-btn:hover
      { transform: translateY(-2px); } .choice-btn.correct { background-color:
      #2ecc71; border-color: #27ae60; } .choice-btn.incorrect {
      background-color: #e74c3c; border-color: #c0392b; } .timer { height: 10px;
      background-color: #3498db; width: 100%; border-radius: 5px; margin-bottom:
      20px; transition: width 1s linear; } .player-list { max-height: 400px;
      overflow-y: auto; } .player-item { display: flex; justify-content:
      space-between; padding: 10px 15px; border-bottom: 1px solid #eee; }
      .score-change { font-weight: bold; animation: fadeIn 1s; } @keyframes
      fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-dark mb-4">
      <div class="container">
        <a class="navbar-brand" href="/">QuizMaster</a>
      </div>
    </nav>

    <div class="container">
      {{{body}}}
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"
    ></script>
  </body>
</html>